{
  "name": "pepper",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
  "dev": "concurrently -n backend,frontend -c green,blue \"npm run dev --prefix backend\" \"npm start --prefix frontend\"",
  "test": "echo \"Error: no test specified\" && exit 1"
}
,
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "concurrently": "^9.2.0"
  }
}
